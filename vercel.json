{
    "version": 2,
    "builds": [
        {
            "src": "web/app.py",
            "use": "@vercel/python",
            "config": {
                "maxLambdaSize": "15mb",
                "runtime": "python3.10",
                "installCommand": "pip install pipenv && pipenv install --deploy --system"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/web/app.py"
        }
    ],
    "env": {
        "PYTHONPATH": ".",
        "LD_LIBRARY_PATH": "/var/task/lib"
    }
} 