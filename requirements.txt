shapely==2.0.7
gpxpy==1.5.0
numpy==1.24.3
networkx==3.1
pandas==2.0.3
geopandas==0.13.2
osmnx==1.3.0
flask==3.0.0
python-dotenv==1.0.0

# Add a comment to explain the constraints
# Note: These specific versions are chosen for compatibility and minimal size
# OSMnx is required for OpenStreetMap data processing and network analysis
